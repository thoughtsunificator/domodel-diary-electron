name: domodel-diary-electron # you probably want to 'snapcraft register <name>'
base: core18 # the base snap is the execution environment for this snap
version: '1.0.9' # just for humans, typically '1.2+git' or '1.3.2'
summary: A small diary application made with domodel # 79 char long summary
description: |
  - Calendar
  - Daily notes encrypted using AES
  - Password Protected

grade: stable # must be 'stable' to release into candidate/stable channels
confinement: devmode # use 'strict' once you have the right plugs and slots
